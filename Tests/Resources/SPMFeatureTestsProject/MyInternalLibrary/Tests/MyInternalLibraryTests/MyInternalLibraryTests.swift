import XCTest
@testable import MyInternalLibrary

final class MyInternalLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyInternalLibrary().text, "Hello, World!")
    }
}
